# Using fact check for the frst time
facts("Testing SimpleMesh type ") do
	# Enter your tests here
	context("Check if function createMesh works or not") do
		# ...

	end # Ends context

	context("Check if function nodeLocation works or not") do
		# ..

	end # Ends context

	context("Check if function boundaryNodeInfo works or not") do
		# ...

	end # Ends context

	context("Check if function boundaryEdgeInfo works or not") do
		# ...

	end # Ends context
end # Ends facts