# Advection Physics Documentation

Describe the equation being solved here

```@contents
  Pages = readdir(joinpath(Pkg.dir("PDESolver"), "docs", "src", "solver", "advection"))[map(file -> endswith(file, ".md"), readdir(joinpath(Pkg.dir("PDESolver"), "docs", "src", "solver", "advection")))]
```
